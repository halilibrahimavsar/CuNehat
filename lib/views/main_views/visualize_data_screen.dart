import 'package:flutter/material.dart';

class VisualizeDataScreen extends StatelessWidget {
  const VisualizeDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("data visualization page"),
    );
  }
}
