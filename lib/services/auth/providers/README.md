In this folder you can create providers for your app. For example You can add google-login, facebook-login, etc... To do this, create your provider class which can inherit AuthProvider class. Then you can add required functions for the application. Note that, AuthProvider is abstract class, so you should implement required functions.

After all